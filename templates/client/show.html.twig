{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>ClientNom</th>
                <td>{{ client.ClientNom }}</td>
            </tr>
            <tr>
                <th>ClientPrenom</th>
                <td>{{ client.ClientPrenom }}</td>
            </tr>
            <tr>
                <th>ClientRaisonSociale</th>
                <td>{{ client.ClientRaisonSociale }}</td>
            </tr>
            <tr>
                <th>ClientSiege</th>
                <td>{{ client.ClientSiege }}</td>
            </tr>
            <tr>
                <th>ClientTel</th>
                <td>{{ client.ClientTel }}</td>
            </tr>
            <tr>
                <th>ClientEmail</th>
                <td>{{ client.ClientEmail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_index') }}">back to list</a>

    <a href="{{ path('app_client_edit', {'id': client.id}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
