{%  extends 'base.html.twig' %}
    {% block title %}{{user.username}} Profile{% endblock %}

{% block body %}



    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-sm-9 mt-5">
                
                    <h2 class="display-4"> {{ user.username | upper}} </h2>
                    <div class="col-4">
                        {% if user.image == null %}
                            {% if user.sexe =='M' %}
                                <img src="{{ asset('/ProfileImg/M.jpeg') }}" width="150"
                                    height="150" class="img-fluid my-2 rounded-circle" >
                            {% else %}
                                <img src="{{ asset('/ProfileImg/F.png') }}" width="150"
                                    height="150" class="img-fluid my-2 rounded-circle">
                            {% endif %}

                            {# <img src="https://publicdomainvectors.org/photos/generic-avatar.png" width="150"
                                height="150" class="img-fluid my-2 rounded-circle"> #}
                        {% else %}
                            <img src="{{ asset('/ProfileImg/'~user.image) }}" width="150"
                                height="150" class="img-fluid my-2 rounded-circle" >
                        {% endif %}
                        {# </div> #}
                        <form action="{{ path('upload_image') }}" method="post" enctype="multipart/form-data">
                        <input type="file" name="image" id="image"
                              >
                        <button style='display:inline;' type="submit" class="btn btn-primary btn-sm my-2" ><i class="fa fa-upload"></i></button>
                        </form>
                    </div>
            </div>

    
            
            <div class="col-sm-1 mt-5 align-self-end">
                <a href="{{ path('security_profile_edit' ,{"id": user.id}) }}"><button class="btn btn-success btn-sm" type="button"><i class="fa fa-edit"></i></button></a>
                <a href="{{ path('security_profile_remove' ,{"id": user.id}) }}"><button class="btn btn-danger btn-sm" type="button"><i class="fa fa-trash"></i></button></a>
            </div>
        <div>

        
        <div class="row justify-content-center text-center">
            <div class="col-sm-10 border rounded mt-3">

                <div class="row justify-content-center">
                    <div class="col-sm-6 mt-4 ">
                        <h6>Nom: {{user.LastName }}<h6><br>
                        <h6>Prenom: {{ user.FirstName }}<h6><br>
                        <h6>E-mail: {{ user.Email }}<h6><br>
                        <h6>Telephone: {{ user.tel }}<h6><br>
                    </div>
                    <div class="col-sm-6 mt-4 ">
                        {% if user.Sexe == 'M' %}
                            <h6>Sexe: Male<h6><br>
                        {% elseif user.Sexe == 'F' %}
                            <h6>Sexe: Female<h6><br>
                        {% endif %}
                        <h6>Nationalité: {{ user.Nationalite }}<h6><br>
                        <h6>Matricule: {{ user.Matricule }}<h6><br>
                        <h6>DateEmbauche: {{ user.DateEmbauche is empty ? "" : user.DateEmbauche|date("d/m/Y")}}<h6><br>
                    </div>
                </div>
            </div>
        </div>
        <br><br><br>
    </div>

{% endblock %}